openapi: "3.0.0"
info:
  title: YAML Test API
  version: "1.0.0"
paths:
  /products:
    get:
      summary: List products
    post:
      summary: Create product
      deprecated: true
  /products/{sku}:
    get:
      summary: Get product
    put:
      summary: Update product
