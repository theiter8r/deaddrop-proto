{
  "openapi": "3.0.0",
  "info": { "title": "Test API", "version": "1.0.0" },
  "paths": {
    "/users": {
      "get": { "summary": "List users", "deprecated": false },
      "post": { "summary": "Create user" }
    },
    "/users/{id}": {
      "get": { "summary": "Get user" },
      "delete": { "summary": "Delete user", "deprecated": true }
    },
    "/legacy/report": {
      "get": { "summary": "Old report", "deprecated": true }
    }
  }
}
